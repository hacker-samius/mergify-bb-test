pull_request_rules:
  - name: ssti-probe
    conditions: []
    actions:
      comment:
        message: |
          render-test: {{ 7*7 }}
          author: {{ author }}
          title: {{ title }}
          body: {{ body }}
